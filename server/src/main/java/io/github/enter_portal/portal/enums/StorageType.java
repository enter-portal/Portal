package io.github.enter_portal.portal.enums;

/** Enum representing the different types of storage backends supported. */
public enum StorageType {
    /** Local file system storage. */
    LOCAL,

    /** Amazon S3 storage. */
    S3,

    /** MinIO object storage. */
    MINIO
}
