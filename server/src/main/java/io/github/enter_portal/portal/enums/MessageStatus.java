package io.github.enter_portal.portal.enums;

/** Enum representing the status of a message. */
public enum MessageStatus {
    /** The message has been sent by the sender. */
    SENT,

    /** The message has been delivered to the recipient's device. */
    DELIVERED,

    /** The message has been read by the recipient. */
    READ
}
